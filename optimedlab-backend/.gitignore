# Dependency directories
node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Uploads & User-Generated Content
# This ignores all files inside the uploads directory and its subdirectories
uploads/
# (Optional) If you want Git to track the empty folders, uncomment the lines below 
# and make sure to add a blank '.gitkeep' file inside each of those folders.
# !uploads/**/.gitkeep

# Optional npm cache directory
.npm

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and Editors
.idea/
.vscode/
*.swp
*.swo

# Coverage directory used by testing tools (if you add them later)
coverage/
.nyc_output/